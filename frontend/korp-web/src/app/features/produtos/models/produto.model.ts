export interface iProdutoCriarDTO {
  codigo: string;
  descricao: string;
  saldo: number;
}

export interface iProdutoResponseDTO {
  id: number;
  codigo: string;
  descricao: string;
  saldo: number;
}
